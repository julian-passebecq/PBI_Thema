# Sprint 001 repair batch 1 — developer report

Status: READY_FOR_TEST; R-A through R-C COMPLETE.
Next actor: independent light tester, round 2.
Starting candidate: sprint-001-dev-01, all 18 hashes matched; original manifest, tester round 1 and lead artifacts preserved. Starting app/status and prior gate copied under ignored `test-artifacts/sprint-001-dev-02/`.
Branch: codex/sprint-001-document-reliability. HEAD unchanged at 7d6fbbdc1a35b508d7229688e77c46ea281b5703. Frozen candidate: sprint-001-dev-02; [successor manifest](../artifacts/sprint-001-candidate-dev-02.json).

## Checkpoints

- R-A COMPLETE: safe attribute rendering for conditional rule value/value2, gradient endpoints and adjacent numeric/text override editors. Supported string thresholds retain their type through editing. Numeric-only gradient endpoint values reject with an exact type/path diagnostic; unknown extensions remain inert. Retained malformed override values are escaped, including object values with shadowed conversion methods. Adjacent numeric filter inputs also escaped defensively.
- R-A recovery: pagesOff accepts the existing string-name representation. Empty bookmark display names retain their content warning/fallback; invalid non-string names and invalid IDs still reject. Audit also found arbitrary blank imported preset keys could create a names array rejected by recovery; recovery now accepts all string preset names generated by the importer. No storage key or representation change.
- R-A evidence: `npm exec --yes --package=node@22 -- node test/repairs.js` exit 0, T17 visual/page formatting, T18 hidden-page UI + legacy recovery, T20 bookmark display name. `node test/contracts.js` exit 0. Tests include editor opening after import/reload, parsed downloads, inert marker assertions and unchanged rejected-state snapshots. R-A lifecycle tests intentionally do not claim periodic notification coverage.
- R-B COMPLETE: notifyDocumentChange routes direct text, formatting, filters, bookmarks and page-name edits. All 13 focused repair scenarios PASS on Node 22/file, including normal autosave/history without forcing helpers, focus retention, immediate post-undo edit/redo invalidation, workspace-only silence and stale draft/conversion invalidation.
- Additional F-07 defense: previewValue gates override/scoped data-bar/rule results by the existing target type. Malformed retained content stays exportable with diagnostics, but cannot become preview CSS. Gradient preview validates finite endpoints and colors. A new T17 preview case covers these neighboring sinks before and after reload.
- R-C COMPLETE: mandatory runner includes test/repairs.js on both transports; clean npm ci and complete Node 22 gate passed. Rule controls wrap within narrow editor columns; inspected 420px and 1440px screenshots and asserted usable control geometry. Candidate frozen for independent round 2.

## Mutation caller audit

| Category | Notification path | Focus / read-only behavior |
| --- | --- | --- |
| Cross-filter text, alt text | wireBind oninput → notifyDocumentChange | Does not reconstruct input; alt text blur may redraw through specTouch, signature dedupes it |
| Visual title/purpose/geometry | wireBind light → specTouch → notifyDocumentChange | Existing light canvas redraw preserves inspector inputs |
| Bindings/sort/kind/action/sync/IDs | afterSpec → specTouch → notifyDocumentChange | Existing committed-action redraw |
| Visual/page formatting add/edit/remove, presets, rules, gradient | wireFmt done → notifyDocumentChange | Commit/change redraw; no character stripping or string-to-number coercion of string rule inputs |
| Report/page/visual filter add/change/remove | wireFilters done → notifyDocumentChange | Covers field/type/value/condition/hidden/locked mutations |
| Bookmark add/rename/page/capture flags/hidden-visual/remove | wireBookmarks done → notifyDocumentChange | Preview-only handler stays separate and silent |
| Page title | bindSpecEditor oninput → notifyDocumentChange | Existing title/label refresh; input node retained |
| Page size/archetype, create/duplicate/delete, visual layout/drag/nudge, gap capture and alt-text generation | specTouch/afterSpec → notifyDocumentChange | Drag remains coalesced at pointer end; no per-frame polling |
| Theme controls, palettes/presets/story/object layouts, page-name hiding | renderJson compatibility path → notifyDocumentChange | Existing synchronous control render paths retained; unchanged signature creates no document event |
| Prepared imports/recovery | applyPrepared explicit revision/history/save finalization | Notification suppressed during application/rollback; same applies during histGo restoration |
| Selection, panel/page browsing, bookmark preview, Detail, layout zoom | No explicit notification; any render fallback detects no change | No new history entry, revision or autosave from document notification |

The notifier observes the canonical signature once for revision changes, refreshes spec diagnostics and schedules history (420ms) and save (700ms). Repeated render calls do not count as additional accepted changes. No polling was added. Pending accepted edits expose Undo and suppress Redo immediately; snapshots still coalesce normally.

## Unrelated audit observation

`bmFromHidden` (Capture what is hidden now) is rendered but has no handler in the existing app. It is an inert button, not a missed notification on a mutation. Deferred to bookmark authoring backlog (S004); no capture semantics are invented in this repair.

No deployment, push, merge, reset, framework/source split or S002 work. No independent acceptance claimed.

## Repair outcome and acceptance evidence

| Finding | Implementation / required evidence | Outcome |
| --- | --- | --- |
| F-07 | Escaped editor values independently of diagnostics; typed preview consumption; finite numeric gradient endpoints with exact paths; string rule intent retained. T17 + retained T01/T07/T16; AC-01/02 and INV-10 | Fixed awaiting independent round 2 |
| F-08 | Existing string page-name recovery contract; actual hide/save/reload and legacy session. T18; AC-03/07 | Fixed awaiting independent round 2 |
| F-09 | Caller audit above; real UI edits reach normal bounded history/save, focus retained, undo/edit invalidates redo, workspace actions quiet. T19; AC-05/06/07 | Fixed awaiting independent round 2 |
| F-10 | Empty display name survives save/reload/export/reimport with warning; missing/empty/non-string IDs and non-string names reject. T20; AC-01/03/07 | Fixed awaiting independent round 2 |

Final environment: Windows, Node 22.23.2, npm 10.5.0, Playwright 1.63.0, Chromium. Commands: `npm exec --yes --package=node@22 -- npm ci` (exit 0), then `npm exec --yes --package=node@22 -- npm test` (exit 0). Final log: `test-artifacts/sprint-001-dev-02/npm-test-final.log`; install log: `test-artifacts/sprint-001-dev-02/npm-ci.log`. Pure contracts passed; smoke, regression, reliability (including conversion, invalid originals, immediate undo/reload and failure injection), and all 13 repair scenarios passed on each of file and HTTP. No page errors were observed by the repair assertions. AC-08/09 have developer gate evidence, pending independent verification.

Repair results/screenshots are under `test-artifacts/sprint-001-dev-02/file/` and `http/`; retained reliability gate output is under `test-artifacts/sprint-001-dev-02/gate/`. Earlier exploratory logs/failure screenshots remain distinguishable from the final passing log. An initial test attempted to edit after Undo without reselecting the visual; corrected setup now reselects before editing. An exploratory full gate exposed squeezed rule controls; wrapping fixed this before the final gate. No product/test changes followed the final passing gate.

T19 mutation assertions use actual UI interactions and wait for observable canonical storage/history without calling histPush, flushSession or renderAll between edit and assertion. The pending-conversion race specifically dispatches a queued DOM input event while the modal is open; it is a handler-race probe, not a pointer interaction through a modal. R-A lifecycle cases are supplemented by independent normal-timer T19 checks, rather than being presented as proof of autosave wiring.

## Round-2 handoff

**READY FOR LIGHT TESTER — Sprint 001 repair batch 1**

Verify all successor manifest hashes, then run the full mandatory gate and T17–T20 with an independent adjacent case per repaired area. Inspect notification assertions for helper-induced saves. Record results in `SPRINT-001-test-round-2.md`, preserving round 1 and the original lead evidence; update candidate/backlog records and return a review packet to the lead or one batched developer repair list. No tester has been launched; this is the user-assigned role handoff defined in PROMPTS.md.

Limitations: no Power BI Desktop/Microsoft-schema compatibility, PbiBench, non-Chromium coverage or remote CI for this dirty candidate established. Unrelated source-only bookmark button observation is deferred as AUT-006. All three repair passes completed without user resumptions. Lead acceptance remains pending.
